import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="filters"
export default class extends Controller {
  connect() {
    // static targets = ["eventType"]

    // filter() {
    //   this.eventTypeTarget.Classlist.toggle
    // }
  }
}
