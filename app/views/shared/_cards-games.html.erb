<!-- _game.html.erb -->

<div class="offset-top">
  <div class="card-container">
    <% games.each do |game| %>
      <div class="card-games" style="background-image: url('<%= game.image_url.presence || "https://c.clc2l.com/t/m/o/monopoly-cgwI69.png" %>')">
        <div class="overlay"></div>
        <h2><%= game.name %></h2>
        <%= button_to "☆", favorite_path(game), method: :patch, class: "btn btn-outline-warning" %>
      </div>
    <% end %>
  </div>
</div>
