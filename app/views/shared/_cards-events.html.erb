<div class="offset-top">
  <div class="card-container">
    <% events.each do |event| %>
      <div class="card-games" style="background-image: url('<%= event.image_url.presence || "https://c.clc2l.com/t/m/o/monopoly-cgwI69.png" %>')">
        <div class="overlay"></div>
        <h2><%= event.name %></h2>
        <%= button_to "☆", favorite_path(event), method: :patch, class: "btn btn-outline-warning" %>
      </div>
    <% end %>
  </div>
</div>
<!-- current_user.events_participations -->
<!-- current_user.events -->
<!-- current_user.games -->
